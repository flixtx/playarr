# MongoDB Configuration
# MongoDB connection URI
# Default: mongodb://localhost:27017
MONGODB_URI=mongodb://localhost:27017

# MongoDB database name
# Default: playarr
MONGODB_DB_NAME=playarr

# Server Configuration
# API server port
# Default: 3000
PORT=3000

# CORS Configuration
# Comma-separated list of allowed origins (leave empty or unset for all origins)
# Example: http://localhost:3000,https://example.com
# Default: true (allows all origins)
CORS_ORIGINS=

# Default Admin User Configuration
# Default admin username (created on first startup if user doesn't exist)
# Default: admin
DEFAULT_ADMIN_USERNAME=admin

# Default admin password (REQUIRED - must be set for production)
# This user is created on first startup if no users exist
DEFAULT_ADMIN_PASSWORD=
