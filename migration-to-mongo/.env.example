# MongoDB Connection
# MongoDB connection string
# Examples:
#   - Local: mongodb://localhost:27017
#   - With auth: mongodb://username:password@localhost:27017
#   - Replica set: mongodb://host1:27017,host2:27017/?replicaSet=rs0
#   - Atlas: mongodb+srv://username:password@cluster.mongodb.net/
MONGODB_URI=mongodb://localhost:27017

# MongoDB Database Name
# The database where migrated data will be stored
MONGODB_DB_NAME=playarr

# Data Directory
# Path to the directory containing JSON data files
# Relative paths are resolved relative to the CURRENT WORKING DIRECTORY
# (where you run the migration script from, not the script file location)
# Examples:
#   - Relative: ./data (if running from project root)
#   - Relative: ../data (if running from migration-to-mongo directory)
#   - Absolute: C:\Git\GitHub\playarr\data (Windows)
#   - Absolute: /home/user/playarr/data (Linux/Mac)
# Default: ./data
DATA_DIR=./data

# Batch Size
# Number of documents to process in each batch
# Larger batches = faster but more memory usage
# Default: 1000
BATCH_SIZE=1000

# Log Level
# Logging verbosity: debug, info, warn, error
# Default: info
LOG_LEVEL=info

# Dry Run Mode
# Set to 'true' to test migration without writing to database
# Set to 'false' or leave empty to perform actual migration
# Default: false
DRY_RUN=false
