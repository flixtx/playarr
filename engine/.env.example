# MongoDB Configuration
# MongoDB connection URI
# Default: mongodb://localhost:27017
MONGODB_URI=mongodb://localhost:27017

# MongoDB database name
# Default: playarr
MONGODB_DB_NAME=playarr

# Directory Configuration
# Cache directory path (relative to engine directory or absolute)
# Default: ../cache
CACHE_DIR=../cache

# Data directory path (relative to engine directory or absolute)
# Default: ../data
DATA_DIR=../data

# Logs directory path (relative to engine directory or absolute)
# Default: ../../logs
LOGS_DIR=../../logs

# Cache Purge Configuration
# Enable automatic cache purging (set to 'true' to enable)
# Default: false (disabled)
CACHE_PURGE_ENABLED=false
